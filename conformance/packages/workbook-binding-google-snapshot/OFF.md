# Workbook Binding Google snapshot fixture

This package binds a neutral subject to an authored locator in an immutable
local XLSX snapshot. The Google Sheets URL is an inert remote descriptor and is
not fetched or compared with the snapshot.
