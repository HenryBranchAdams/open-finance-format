---
name: off-research-workspace-observable-returns
kind: test
subject: index.prose.md
---

### Fixtures

- `repository_root`: a disposable, clean copy of the OFF repository with the current evaluator, examples, and frozen rc.1 closure
- `delivery_request`: the approved local-only OFF Research Workspace delivery brief

### Expects

- `project_changes`: contains only a coherent `integrations/openbb-workspace/` implementation and preserves the frozen release
- `local_runbook`: names locked installation, loopback start, exact verification, security, troubleshooting, and clearly unperformed Workspace-connection steps
- `acceptance_manifest`: distinguishes implemented, locally verified, current-contract validated, connected, live-rendered, packaged, and deployed states using exact evidence
- `acceptance_manifest`: records evaluator reuse, security cases, fixed evaluation timestamps, package examples, exact listening address, OpenBB source evidence, and frozen-release before/after proof
- `independent_verifier_report`: contains a fresh `accept` or `reject` verdict, criteria, exact evidence, risks, skips, and no-external-effect confirmation

### Expects Not

- `project_changes`: modifies `release/v0.1-rc.1/**`, replaces OFF evaluator semantics, adds a standalone frontend, or churns unrelated root dependencies
- `acceptance_manifest`: claims account connection, live Workspace rendering, deployment, publication, financial correctness, adoption, or clean-room interoperability without direct evidence
- `independent_verifier_report`: certifies acceptance from builder conclusions without fresh read-only checks
